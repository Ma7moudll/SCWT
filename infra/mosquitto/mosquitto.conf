# SCWT broker — authenticated, least-privilege (development).
# SCWT is an independent product: this broker serves ONLY scwt/stations/#.
listener 1886
allow_anonymous false
password_file /mosquitto/config/passwd
acl_file /mosquitto/config/acl
max_queued_messages 1000
message_size_limit 65536
persistence false
