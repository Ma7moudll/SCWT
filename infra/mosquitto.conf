# Ecolamp broker — authenticated, least-privilege (development).
# Ecolamp is an independent product: this broker serves ONLY ecolamp/stations/#.
listener 1886
allow_anonymous false
password_file /mosquitto/config/passwd
acl_file /mosquitto/acl
max_queued_messages 1000
message_size_limit 65536
persistence false
